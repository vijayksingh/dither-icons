import {mail} from './mail';
import {message} from './message';
import {send} from './send';
/** Each correspondence gesture keeps its own storyboard and geometry. */
export const communication={mail,message,send};
